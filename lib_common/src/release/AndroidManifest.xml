<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lwkandroid.lib.common">

    <application>
        <provider
            android:name=".DebugToolProvider"
            android:authorities="${applicationId}.DebugToolProvider" />
    </application>

</manifest>
