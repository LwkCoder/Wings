<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lwkandroid.demo">

    <application
        android:name=".app.DemoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".dialog.DialogActivity"
            android:screenOrientation="portrait"
            android:theme="@style/BaseActivityStyle"></activity>
        <activity
            android:name=".pop.PopActivity"
            android:screenOrientation="portrait"
            android:theme="@style/BaseActivityStyle" />
        <activity
            android:name=".rxhttp.login.LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/BaseActivityStyle" />
        <activity
            android:name=".rxhttp.RxHttpActivity"
            android:screenOrientation="portrait"
            android:theme="@style/BaseActivityStyle" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/BaseActivityStyle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>