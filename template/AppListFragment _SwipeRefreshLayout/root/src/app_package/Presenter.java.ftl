package ${packageName};

/**
 * Description:Presenter层
 * @author
 * @date
 */
class ${fragmentClass}Presenter extends ${fragmentClass}Contract.Presenter
{
    @Override
    public ${fragmentClass}Contract.Model createModel()
    {
        return new ${fragmentClass}Model();
    }
}
