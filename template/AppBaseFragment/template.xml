<?xml version="1.0"?>
<template
    format="5"
    revision="5"
    name="AppBaseFragment with MVP Template(Java)"
    minApi="16"
    minBuildApi="16"
    description="Creates a new AppBaseFragment with MVP">

    <category value="Fragment" />

    <parameter
        id="className"
        name="Fragment Name"
        type="string"
        constraints="class|nonempty|unique"
        default="AppFragment"
        help="The name of the fragment class to create" />

    <parameter
        id="includeLayout"
        name="Create layout XML?"
        type="boolean"
        default="true"
        help="Generate a layout XML for the fragment" />
		
    <parameter
        id="fragmentName"
        name="Fragment Layout Name"
        type="string"
        constraints="layout|nonempty|unique"
        default="fragment_app"
        visibility="includeLayout"
        suggest="fragment_${classToResource(className)}"
        help="The name of the layout to create" />
		
    
    <thumbs>
        <!-- default thumbnail is required -->
        <thumb>Fragment_template.png</thumb>
    </thumbs>

    <globals file="globals.xml.ftl" />
    <execute file="recipe.xml.ftl" />

</template>
